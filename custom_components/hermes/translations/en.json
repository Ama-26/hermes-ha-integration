{
  "config": {
    "step": {
      "user": {
        "title": "Connect Hermes",
        "description": "Connection details for the Hermes Gateway API server.",
        "data": {
          "host": "Host (IP or hostname)",
          "port": "Port",
          "api_key": "API key (API_SERVER_KEY)",
          "profile": "Profile",
          "model": "Model (optional)",
          "provider": "Provider (optional)"
        },
        "data_description": {
          "host": "The IP of the server running the Hermes gateway.",
          "port": "Default: 8642",
          "api_key": "The API_SERVER_KEY from the Hermes .env.",
          "profile": "Which Hermes profile to use (e.g. default).",
          "model": "Model override, e.g. google/gemini-2.5-flash. Empty = server default.",
          "provider": "Provider for the model, e.g. openrouter. Empty = server default."
        }
      },
      "reconfigure": {
        "title": "Reconfigure Hermes",
        "description": "Update the connection details for the Hermes Gateway API server.",
        "data": {
          "host": "Host (IP or hostname)",
          "port": "Port",
          "api_key": "API key (API_SERVER_KEY)",
          "profile": "Profile",
          "model": "Model (optional)",
          "provider": "Provider (optional)"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the Hermes server.",
      "invalid_auth": "Invalid API key.",
      "unknown": "Unexpected error."
    },
    "abort": {
      "already_configured": "This profile is already configured on this server."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Hermes Options",
        "data": {
          "timeout": "Request timeout (seconds)",
          "model": "Model override (optional)",
          "provider": "Provider (optional)"
        },
        "data_description": {
          "model": "e.g. google/gemini-2.5-flash. Leave empty for server default.",
          "provider": "e.g. openrouter. Leave empty for server default."
        }
      }
    }
  },
  "entity": {
    "binary_sensor": {
      "connected": { "name": "Hermes Connected" }
    },
    "sensor": {
      "latency": { "name": "Hermes Latency" },
      "model": { "name": "Hermes Model" }
    }
  }
}
